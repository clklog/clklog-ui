// module.exports = {
//   baseApi: "https://demo.clklog.com/api",
// };

// // 将配置信息添加到 window 对象上
// if (typeof window !== 'undefined') {
  // window.globalConfig = {
  //   BASE_API: "https://demo.clklog.com/api", // 这里可以根据需要覆盖或者扩展配置
  // };
// }
window.globalConfig = {
  BASE_API: "https://demo.clklog.com/api", // 这里可以根据需要覆盖或者扩展配置
};